{
  "name": "slide-server",
  "version": "1.0.0",
  "description": "Server for Slide Solutions SPA",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --inspect index.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zack Woo",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.170.0",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "engines": {
    "node": "16.13.2",
    "npm": "8.1.2"
  }
}
